services:
  qdrant:
    image: qdrant/qdrant:v1.19.1
    container_name: bill-assistant-qdrant
    ports:
      - "6333:6333" # REST
      - "6334:6334" # gRPC (used by Qdrant.Client)
    volumes:
      - qdrant_storage:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_storage:
